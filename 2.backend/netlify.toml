[build]
  publish = "."
  functions = "netlify/functions"

# API routes
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
  force = true

# Frontend routes (IMPORTANT FIX)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
